# 行动勇者 · 2.5D 游戏化改造 总体设计规划

> 文档类型：游戏交互 / 视觉 / 实现 总体规划（不直接产代码，仅规划 + 下游提示词）
> 面向读者：产品/设计（对齐）、**Miora**（出视觉资产）、**CodeBuddy**（做实现）
> 当前项目：行动勇者 · 一个会生长的世界（ADHD 友好型游戏化生活/工作规划）
> 核心设定：现实行动 → 世界获得光 → 打印机吐出实物小票

---

## 0. 一句话定位

把现在的"多标签网页应用"改造成一本**会生长、能走进去的童话立体故事书**：玩家是勇者，待办事项是需要被驱逐的"拖延阴影"，伙伴陪你冒险，每一次真实行动都会让 2.5D 世界更亮一点，并吐出一张实体战利品小票。

---

## 1. 设计理念与基调

### 1.1 为什么是「2.5D」而不是 2D 或 3D
- **2D（现状）**：信息清楚，但缺乏"世界感"和"我在冒险"的沉浸。
- **3D（全三维）**：沉浸强，但建模重、加载重、ADHD 用户容易在复杂空间里迷失、焦虑。
- **2.5D（推荐）**：用等距/立体书视角的"纸艺 diorama"呈现——既有纵深、可镜头推进，又保持扁平可爱、轻量、低压力。契合"温暖治愈 + 童话故事书"的调性，且对性能和可访问性友好。

### 1.2 体验目标（ADHD 优先）
1. **即时正反馈**：动作要"看得见发生"，不是按钮一按就跳数字。
2. **低认知负荷**：每个地点只做一件事，导航一眼可懂。
3. **温暖不施压**：失败/拖延不被惩罚，伙伴永远温柔接住你。
4. **实体锚点**：小票把虚拟行动落到现实，强化"我真的做了"。

### 1.3 视觉基调关键词（给所有人的统一锚）
`童话故事书立体场景` · `纸艺拼贴(papercraft)` · `温暖治愈` · `柔和暖光` · `2.5D 等距视角` · `圆润可爱` · `低饱和暖色调` · `轻微投影/纸层叠` · `微噪点纸质纹理`

---

## 2. 世界观与叙事框架

| 概念 | 游戏化映射 | 现有数据来源 |
|---|---|---|
| 勇者（你） | 玩家角色，有等级/经验 | `profiles.level / xp` |
| 伙伴 | 同行小动物，需喂养/陪伴 | `companion`（name/stage/hunger/mood/growth） |
| 拖延阴影 = 敌人 | 每一个待办任务 | `tasks`（title/type/difficulty） |
| 世界之光 | 行动积累的光，让地图变亮 | `world.light` |
| 领地 | 连续行动解锁的地块 | `territory.streak / unlocked_tiles` |
| 图鉴 | 战斗掉落的藏品 | `collection` |
| 编年史 | 世界的历史书（日记/复盘） | `chronicle` |
| 贤者之书 | 答案之书（随机鼓励语） | `data/answers.json` |
| 小票 | 实体战利品 | `printer` + `receipt.js` |
| 季节/昼夜 | 世界氛围 | `world.season / day_count / phase` |

---

## 3. 2.5D 世界地图与区域规划

### 3.1 地图总览
- 一张**等距视角的圆形小岛 / 立体书跨页**，中心是「勇者冒险基地」Hub。
- 岛屿随 `world.light` 增长而**变亮、开花、解锁新地块**（`territory`）。
- 镜头可平移/缩放；点击地点 → 镜头平滑推近 → 进入该模块的"房间/页面"。
- 昼夜与季节改变天光颜色与植被（春樱 / 夏绿 / 秋枫 / 冬雪），但始终是暖光。

### 3.2 地点 ↔ 现有模块 映射
> 默认按现有 6 个视图 + 打印机模块映射；如你心中的"项目"另有所指，替换地点名与对应数据即可。

| 地点（游戏内） | 对应模块 | 入口功能 | 视觉意象 |
|---|---|---|---|
| 🏕️ 出征营地 | `tasks` 勇者之路 | 接任务 / 看进行中 / 触发战斗 | 帐篷、木牌任务栏、篝火 |
| 🏡 伙伴小屋 | `companion` | 看伙伴状态 / 对话 | 圆窗小屋、菜园、吊床 |
| 🌷 领地花田 | `world` + `territory` | 看世界之光 / 连续天数 / 解锁 | 发光花田、拼图地块 |
| 📚 故事书屋 | `chronicle` 编年史 | 写日记 / 今日复盘 / 打印 | 书架、蜡烛、羊皮卷 |
| 🔮 预言神泉 | `oracle` 贤者之书 | 翻页求答案 | 喷泉、发光书页、星点 |
| 🖼️ 图鉴陈列馆 | `collection` | 看已收集藏品 | 玻璃展柜、漂浮藏品 |
| 🖨️ 小票工坊 | `printer` | 连接打印机 / 吐小票 | 木质工坊、传送带、小票机 |

### 3.3 导航交互
- Hub 地图 = 主视图（`view-world` 升级为可交互 2.5D 地图）。
- 点击地点：镜头推近 + 该地点 UI 面板从"纸面"掀开（翻书动效）。
- 返回：点击"合上书"或地图缩略图回到 Hub。
- 顶部常驻 HUD（见 §7），任何地点都不遮挡核心信息。

---

## 4. 角色系统与动作设计（重点：不可静态）

所有角色都需要**至少 idle 循环动画 + 触发动作**，禁止纯静态贴图。

### 4.1 勇者（主角）动作集
| 动作 | 触发 | 说明 |
|---|---|---|
| `idle` | 待机 | 呼吸起伏、发梢/披风轻飘 |
| `walk` / `run` | 走动/上前 | 步伐、身体上下微弹 |
| `attack_slash` | 出剑 | 挥砍弧光、运动模糊、收招回弹 |
| `charge` | Boss 蓄力 | 蹲身、光粒聚拢 |
| `hit` | 被击中（稀有） | 后仰、闪红 |
| `victory` | 胜利 | 举剑、跳起、转圈 |
| `cheer` | 伙伴互动 | 挥手、比心 |
| `think` | 贤者之书 | 托腮、问号气泡 |

### 4.2 伙伴（companion）动作集
`idle`（摇尾巴/扑蝶）· `eat`（吃食物条）· `happy`（蹦跳）· `sleep`（Zzz）· `worry`（抱膝）· `evolve`（进化光效，按 stage）

### 4.3 敌人 = 拖延阴影
- 由任务生成：难度 1~5 决定体型/狰狞度，但**永远保持可爱**（毛球怪、墨渍怪、拖延史莱姆）。
- 动作：`spawn`（从纸面 pop-up 长出）· `taunt`（抖一抖、冒问号）· `flinch`（受击闪白后仰）· `defeated`（化作星点消散，掉 1 件藏品）

### 4.4 动画实现建议（给 CodeBuddy）
- **首选**：Miora 产出角色**多姿势 Pose Sheet（透明 PNG 序列）** → CodeBuddy 切帧做成 sprite-sheet，用 Web Animations API / CSS steps() 播放。轻量、跨端、零依赖。
- **进阶**：若需更顺滑的肢体动作，用 **Lottie（JSON 矢量动画）** 承载角色与特效用；或用骨骼动画（Spine/DragonBones）。
- 全程避免"贴一张图不动"；每个角色至少 idle 循环。

---

## 5. 对战 / 战斗过程设计（重点：有角色动作，非静态）

### 5.1 触发
- 在「出征营地」点任务「完成」→ 不立刻弹结算，而是**进入战斗过场**（复用 `game.completeTask` 的结算逻辑，但把"数字变化"包进演出里）。

### 5.2 战斗分镜（Storyboard，按时间顺序）
总时长建议 **普通 2.5~3.5s / Boss 5~7s**（ADHD：短、爽、即时）。

| 阶段 | 画面与动作 | 反馈 |
|---|---|---|
| ① 敌人登场 | 阴影怪从纸面 pop-up 长出（`spawn`），镜头轻微推近 | 音效：轻"啵" |
| ② 勇者上前 | 勇者 `walk` 从画面左走入，镜头跟随 | 脚步沙沙 |
| ③ 蓄力 | 勇者 `charge` 蹲身，光粒聚拢（Boss 更明显） | 蓄力嗡鸣 |
| ④ 攻击 | 勇者 `attack_slash`，剑光弧 + **hitstop 顿帧** + **轻微屏震** | 命中"铮" |
| ⑤ 敌人受击 | 阴影怪 `flinch` 闪白后仰、血条（可爱心数）减少 | 碎裂音 |
| ⑥（Boss 专属） | 多段连击：2~3 次 `attack_slash` + 专属全屏特效能 | 层层爆点 |
| ⑦ 胜利 | 阴影怪 `defeated` 化星点消散；勇者 `victory` 举剑跳起；**光芒爆发 + 粒子** | 胜利号角 |
| ⑧ 伙伴反应 | 伙伴从角落 `cheer` 蹦出鼓掌 | 欢呼声 |
| ⑨ 掉落 | 星点凝成 1 件藏品飞入「图鉴」（collection +1） | 叮~ |
| ⑩ 世界变亮 | 地图整体提亮、`world.light +N` 可视化（光点汇入花田） | 环境光渐亮 |
| ⑪ 实体战利品 | 「小票工坊」传送带吐出实物小票（已连接的打印机真实打印） | 打印机咔哒 |

### 5.3 演出技术要点（给 CodeBuddy）
- **时序状态机**：用 `async/await` + 时间轴（如 GSAP timeline 或自研轻量 sequencer）编排 ①→⑪，每步可插 `await wait(ms)` 与回调。
- **顿帧 hitstop**：攻击命中瞬间全局 `animation-play-state: paused` 60~120ms 再恢复，增强打击感。
- **屏震**：对场景容器施加短促 `transform: translate()` 抖动（CSS keyframes，衰减）。
- **粒子**：轻量 canvas 粒子或 CSS 粒子（光点、星屑），数量克制（ADHD 不喜过载）。
- **与结算对接**：演出只是"包装"，真正的数值写入仍在 `game.completeTask`（XP/light/feed/drop/streak/chronicle）。演出结束再 `render` 刷新各面板。

### 5.4 情绪曲线
平静（接任务）→ 小紧张（敌人登场）→ 爽快（命中/胜利）→ 温暖（伙伴+光+小票）。**全程零惩罚**，拖延/Boss 只是"更值得庆祝的胜利"。

---

## 6. 核心交互循环（与现有架构对齐）

```
玩家在「出征营地」接任务(tasks.insert)
        │
        ▼
真实行动 → 点「完成」→ 触发 2.5D 战斗演出(§5)
        │
        ▼
game.completeTask 结算：profiles.xp↑ / world.light↑ / companion 喂养 / 掉落 / streak↑ / chronicle 记录
        │
        ▼
演出 ⑦~⑪ 把上述数值"演"出来（光、伙伴、图鉴、小票）
        │
        ▼
（若已连打印机）实体小票吐出 —— 行动落回现实
```

各视图 = 地图地点，复用现有数据模型，**不重建后端**，只把"列表页"升级为"地点房间"。

---

## 7. 界面与 UI 组件（童话立体书风格）

- **顶部 HUD**：等级/经验条、世界之光✨、季节/昼夜、伙伴状态小图标。纸质横幅样式，常驻不遮挡。
- **地点导航**：Hub 地图上的地点图标（带可爱招牌），hover 微弹。
- **任务接取面板**（出征营地）：难度选择做成"怪物图鉴卡"，Boss 任务有专属红绸标识。
- **伙伴对话气泡**：手写体气泡，从伙伴头顶冒出。
- **战斗结算弹窗**：翻书页式弹出，展示获得 XP/光/掉落，带 `victory` 小动画。
- **小票预览**：工坊里实时预览即将打印的小票样式。
- **统一规范**：大圆角、纸纹底、暖色描边、圆体/手写风字体、微投影；动效统一 `cubic-bezier` 缓动。

---

## 8. 视觉风格规范（给 Miora 的核心 Brief）

**统一风格锚（每张图都必须带，保证一致性）：**
> 童话故事书立体场景，纸艺拼贴 papercraft 风格，2.5D 等距视角，温暖治愈，柔和暖光从右上打来，圆润可爱造型，低饱和暖色调（奶油米、蜜桃粉、薄荷绿、暖姜黄、淡雾蓝），轻微纸层叠投影，细腻纸质噪点纹理，画面干净留白，带有被翻开的故事书跨页质感，禁止恐怖/暗黑/高对比冷色。

**色板（建议 Hex）：**
- 奶油底 `#FBF3E4` · 暖木 `#C9A27A` · 蜜桃 `#F6C9B0` · 薄荷 `#BFE3C9` · 姜黄 `#F2C879` · 雾蓝 `#A9C7D9` · 描边 `#6B4F3A`

**构图**：立体书跨页 / 圆形小岛 diorama，前中后景分层（parallax 友好）。

**角色设计 brief**：
- 勇者：圆脸、披风、小剑，表情坚定但柔和，可换色。
- 伙伴：圆滚滚毛球小动物，随 stage 进化（幼态→成态）。
- 阴影怪：由"待办"具象化的可爱怪（毛球/墨渍/史莱姆），难度越高越大但不吓人。
- NPC/藏品：图谱式可爱小物件。

**禁止项**：恐怖、血腥、尖锐、高饱和冷色、密集信息、强闪光（ADHD 友好）。

---

## 9. Miora 视觉生成提示词（可直接喂）

> 使用说明：每张图前都带 §8 的统一风格锚；尺寸建议 16:9（场景）/ 1:1（角色）/ 透明背景（角色与道具）。可分批生成。

### 9.1 世界地图总览
```
童话故事书立体场景，纸艺拼贴 papercraft 风格，2.5D 等距视角，温暖治愈。一张被翻开的故事书跨页上，漂浮着一座圆形小岛"勇者冒险基地"：中央是篝火营地 Hub，周围环绕七个小地点（出征营地帐篷、伙伴小屋、发光花田、故事书屋、预言神泉、图鉴陈列馆、小票工坊），由鹅卵石小路相连。柔和暖光从右上打来，奶油米底色，蜜桃粉/薄荷绿/暖姜黄/淡雾蓝低饱和配色，轻微纸层叠投影与纸质噪点，画面干净，禁止暗黑与高对比冷色。16:9。
```

### 9.2 各地点场景（7 张，分别生成，替换括号内容）
```
童话故事书立体场景，纸艺拼贴 papercraft，2.5D 等距小场景，温暖治愈，柔和暖光。（出征营地：帐篷、木牌任务栏、噼啪篝火、远处小旗）奶油米底，低饱和暖色，纸层投影与噪点，干净留白，禁止暗黑。16:9。
```
（其余 6 地点同理，分别描述：伙伴小屋=圆窗小屋+菜园+吊床；领地花田=发光花田+拼图地块；故事书屋=书架+蜡烛+羊皮卷；预言神泉=喷泉+发光书页+星点；图鉴陈列馆=玻璃展柜+漂浮藏品；小票工坊=木质工坊+传送带+小票机）

### 9.3 角色设定图
```
勇者（主角）：圆脸少年/少女，披风，手持小剑，表情坚定柔和，可换色披风，童话纸艺风，2.5D 等距，透明背景，圆润可爱，暖色描边，禁止尖锐。1:1。
伙伴：圆滚滚毛球小动物，大眼睛，温柔表情，幼态，童话纸艺风，透明背景，暖色。1:1。
阴影怪（拖延史莱姆，难度3）：由"待办"具象化的可爱墨渍怪，圆润不吓人，半透明，童话纸艺风，透明背景。1:1。
阴影怪 Boss 版（难度5）：更大更华丽的拖延之王，但仍可爱，童话纸艺风，透明背景。1:1。
```

### 9.4 战斗关键帧 Pose Sheet（给 CodeBuddy 切帧用）
```
勇者动作序列 Pose Sheet，透明背景，统一纸艺童话风与暖色描边：一行 6 格依次展示 idle 呼吸 / walk 迈步 / charge 蓄力聚光 / attack_slash 挥砍弧光 / hit 后仰 / victory 举剑跳起。等距视角，角色居中，间距均匀，禁止多余背景。长横图。
伙伴 cheer Pose Sheet：happy 蹦跳 / cheer 挥手比心 / worry 抱膝 / sleep Zzz，透明背景，长横图。
阴影怪 flinch 序列：taunt 抖一抖 / flinch 闪白后仰 / defeated 化星点消散，透明背景，长横图。
```

### 9.5 UI 组件套件
```
童话故事书 UI 组件套件，纸艺风，2.5D，温暖治愈：顶部纸质横幅 HUD（等级/经验条/世界之光/季节）、任务卡片、对话气泡、翻书页结算弹窗、小票预览卡、地点招牌图标（7 个）。奶油米底，暖木描边，圆角，低饱和暖色，统一字体感，透明或纯色背景，禁止冷色与尖锐边角。拼板图。
```

---

## 10. CodeBuddy 实现提示词（可直接喂）

> 总体约束：保留现有"原生 HTML/CSS/JS + ES Modules、无构建步骤"技术栈；通过 ESM CDN（如 `https://esm.sh/`）引入所需库，**不要引入打包器**；复用 `js/` 下现有模块与 `supabase/` 后端，不重建数据层。

### 10.1 整体架构与技术选型
```
把现有多标签应用改造成 2.5D 童话立体书风格的游戏化界面，保留原生 HTML/CSS/JS + ES Modules、无构建步骤。
技术选型（请按此实现）：
- 世界地图与地点：用 CSS 3D transform 搭建"纸艺 diorama"（多层平面 + perspective + 轻微 rotateX），地点为可点击的 DOM 节点；镜头推近用 transform/translate+scale 过渡。若需要更顺滑的相机与深度，可改用 Three.js 正交相机（orthographic）渲染地图、角色用 billboard 精灵，仍经 ESM CDN 引入、无打包。
- 角色与动作：Miora 提供的 Pose Sheet PNG → 切帧成 sprite-sheet → 用 CSS steps() 或 Web Animations API 播放 idle/walk/attack 等动画；每个角色至少要有 idle 循环，禁止纯静态贴图。
- 演出编排：自研轻量时间轴 sequencer（async/await + wait(ms)）或引入 GSAP timeline（ESM CDN），编排战斗 ①~⑪ 阶段。
- UI 面板：每个地图地点对应一个现有视图（tasks/companion/world/chronicle/oracle/collection/printer），以"翻书页"动效从纸面掀开，复用 main.js 现有渲染函数。
保持 ADHD 友好：动效短（战斗 2.5~7s）、即时反馈、零惩罚、可访问性（尊重 prefers-reduced-motion）。
```

### 10.2 世界地图导航与地点切换
```
实现 2.5D 世界地图（见 10.1 选型）：
- Hub 主视图展示圆形小岛与 7 个地点节点（出征营地/伙伴小屋/领地花田/故事书屋/预言神泉/图鉴陈列馆/小票工坊），节点由现有视图 id 映射。
- 点击地点：镜头平滑推近（transform transition 300~500ms），该地点 UI 面板以翻书页动效展开；提供"合上书/地图缩略图"返回 Hub。
- 地图随 world.light 与 territory.streak 变化：light 越高整体越亮、花田越盛；streak 解锁更多地块（显隐对应节点）。
- 昼夜/季节（world.season/phase）改变天光滤镜（CSS filter 或叠加渐变），保持暖色。
把现有 showView() 改为地图驱动的地点进入，保留各 renderXxxView() 函数。
```

### 10.3 角色动画系统
```
实现轻量角色动画系统：
- 输入：Miora 提供的角色 Pose Sheet（透明 PNG，等距视角，横排多格）。
- 用 canvas 或 CSS background-position steps() 将 Pose Sheet 切帧播放；封装 play(action, {loop, speed, onEnd})。
- 预定义动作：勇者 idle/walk/run/attack_slash/charge/hit/victory/cheer/think；伙伴 idle/eat/happy/sleep/worry/evolve；阴影怪 spawn/taunt/flinch/defeated。
- 所有角色在场景中至少持续播放 idle；战斗时切换到触发动作并回归 idle。
- 支持 prefers-reduced-motion：降级为最少帧/静态。
```

### 10.4 战斗演出引擎（重点）
```
实现战斗演出引擎，触发自 tasks 视图的"完成"按钮（复用 game.completeTask 结算，但把数字变化包进演出）：
按时间顺序编排阶段（总时长 普通 2.5~3.5s / Boss 5~7s）：
① 阴影怪 spawn（从纸面 pop-up，CSS scale+opacity）② 勇者 walk 上前（sprite 位移）③ charge 蓄力（光粒聚拢）④ attack_slash（剑光弧 + hitstop 顿帧 60~120ms + 轻微屏震 transform 抖动衰减）⑤ 阴影怪 flinch（闪白后仰，心数血条减少）⑥ Boss 多段连击 2~3 次 ⑦ victory（阴影怪化星点消散 + 勇者 victory + 光芒爆发粒子）⑧ 伙伴 cheer ⑨ 掉落藏品飞入图鉴 ⑩ 世界变亮（world.light+N 可视化，光点汇入花田）⑪ 小票工坊吐出实物小票（已连打印机则真实打印）。
实现要点：用 async/await 时间轴编排；hitstop 用 animation-play-state 暂停再恢复；屏震对场景容器施加衰减 transform；粒子用轻量 canvas/CSS（数量克制）；演出结束调用现有 render 刷新各面板。数值写入仍走 game.completeTask（xp/light/feed/drop/streak/chronicle），演出仅做表现层。
```

### 10.5 各模块地点接入
```
将现有 6 个视图 + 打印机模块分别接入对应地点房间，UI 升级为童话纸艺风但保留功能：
- 出征营地 ↔ tasks（接任务/进行中/触发战斗）
- 伙伴小屋 ↔ companion（状态条 + 对话气泡，伙伴 sprite 实时播放动作）
- 领地花田 ↔ world+territory（世界之光/连续天数/解锁地块可视化）
- 故事书屋 ↔ chronicle（日记/复盘/打印）
- 预言神泉 ↔ oracle（翻页求答案，勇者 think 动作）
- 图鉴陈列馆 ↔ collection（藏品陈列，掉落时飞入动画）
- 小票工坊 ↔ printer（连接/预览/吐小票）
复用 main.js 的 renderXxxView 与 game/companion/world/chronicle/collection/territory/oracle/receipt/printer 模块，不重建逻辑。
```

### 10.6 与现有 Supabase / 打印机逻辑对接
```
严格复用现有数据层，不新建表：
- 战斗结算调用 game.completeTask（已含 xp/light/feed/drop/streak/chronicle 写入）。
- 地图亮度/解锁读取 world.light 与 territory.streak/unlocked_tiles。
- 伙伴动作状态读取 companion（hunger/mood/growth/stage）。
- 小票打印沿用 printer.js（Web Bluetooth）+ receipt.js（celebrate/review/oracle），演出 ⑪ 调用既有打印流程。
- 答案之书沿用 data/answers.json + oracle.js。
仅在表现层（动画/地图/UI）新增代码，逻辑层零改动或最小改动。
```

### 10.7 性能与无障碍（ADHD 友好）
```
- 动效尊重 prefers-reduced-motion，提供"减少动作"开关（可存 localStorage，类比现有 autoPrint）。
- 战斗演出短而爽，避免过长等待；提供"跳过演出"按钮直达结算。
- 角色/粒子数量克制，避免视觉过载；保持 60fps（用 transform/opacity 动画，避免 layout thrash）。
- 地点导航清晰、可键盘可达；文字对比度达标。
- 无构建步骤，依赖经 ESM CDN 引入并做加载失败兜底。
```

---

## 11. 实施路线 / 里程碑

| 阶段 | 交付 | 负责 |
|---|---|---|
| **Phase 0** | Miora 产出视觉资产：地图总览、7 地点、角色 Pose Sheet、战斗关键帧、UI 套件 | Miora |
| **Phase 1** | 静态 2.5D 地图 + 地点导航 + 翻书页面板（先无动画） | CodeBuddy |
| **Phase 2** | 角色动画系统：sprite 切帧、idle/动作播放 | CodeBuddy |
| **Phase 3** | 战斗演出引擎 ①~⑪（含 hitstop/屏震/粒子/掉落/变亮/小票） | CodeBuddy |
| **Phase 4** | 打磨：季节昼夜、无障碍、性能、与打印机/后端联调 | CodeBuddy |

---

## 12. 风险与取舍

- **性能 vs 华丽**：2.5D + 粒子易掉帧 → 以 transform/opacity 为主，粒子克制，提供降级开关。
- **ADHD 友好优先**：任何"爽感"都不能变成"压力"；零惩罚、可跳过、即时反馈是红线。
- **一致性**：所有视觉必须带 §8 统一风格锚，避免 Miora 多批出图风格漂移。
- **可维护性**：逻辑层（Supabase/printer）零改动，2.5D 仅加在表现层，便于回滚。
- **无构建约束**：依赖走 ESM CDN，注意离线/弱网兜底。

---

> 本规划为"设计 + 提示词"文档，不直接产出可运行游戏。下一步建议：先跑 Phase 0 让 Miora 出资产，再按 §10 把提示词交给 CodeBuddy 从 Phase 1 起步。
